class Task{

    constructor(title,desc,startDate,budget,status,color)
{

    this,title = title;
    this,description = desc;
    this,startDate = startDate;
    this,budget = budget;
    this,color = color;
}
greet (){
console.log("hello, this is the" $(this.title));
} 
}